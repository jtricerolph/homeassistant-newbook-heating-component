{
  "config": {
    "step": {
      "user": {
        "title": "Newbook API Connection",
        "description": "Enter your Newbook API credentials",
        "data": {
          "username": "Username",
          "password": "Password",
          "api_key": "API Key",
          "region": "Region"
        }
      },
      "polling": {
        "title": "Polling Settings",
        "description": "Configure how often to check for booking updates",
        "data": {
          "scan_interval": "Refresh interval (minutes)"
        }
      },
      "defaults": {
        "title": "Default Room Settings",
        "description": "These settings will be applied to all discovered rooms (can be adjusted per room later)",
        "data": {
          "default_arrival_time": "Default arrival time",
          "default_departure_time": "Default departure time",
          "heating_offset_minutes": "Heating offset (minutes before arrival)",
          "cooling_offset_minutes": "Cooling offset (minutes after departure, can be negative)",
          "occupied_temperature": "Occupied temperature (°C)",
          "vacant_temperature": "Vacant temperature (°C)"
        }
      },
      "trv_monitoring": {
        "title": "TRV Monitoring Settings",
        "description": "Configure TRV reliability and battery monitoring",
        "data": {
          "max_retry_attempts": "Maximum retry attempts",
          "command_timeout": "Command timeout (seconds)",
          "battery_warning_threshold": "Battery warning threshold (%)",
          "battery_critical_threshold": "Battery critical threshold (%)"
        }
      },
      "valve_sync": {
        "title": "Valve Sync Defaults",
        "description": "Default behavior for room valve synchronization",
        "data": {
          "sync_setpoints_default": "Sync room setpoints by default",
          "exclude_bathroom_default": "Exclude bathroom from sync by default"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to Newbook API",
      "invalid_auth": "Invalid authentication credentials",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "This Newbook account is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Newbook Options",
        "description": "Update integration settings",
        "data": {
          "scan_interval": "Refresh interval (minutes)",
          "default_arrival_time": "Default arrival time",
          "default_departure_time": "Default departure time",
          "heating_offset_minutes": "Heating offset (minutes)",
          "cooling_offset_minutes": "Cooling offset (minutes)",
          "occupied_temperature": "Occupied temperature (°C)",
          "vacant_temperature": "Vacant temperature (°C)",
          "max_retry_attempts": "Maximum retry attempts",
          "command_timeout": "Command timeout (seconds)",
          "battery_warning_threshold": "Battery warning threshold (%)",
          "battery_critical_threshold": "Battery critical threshold (%)",
          "sync_setpoints_default": "Sync room setpoints by default",
          "exclude_bathroom_default": "Exclude bathroom from sync by default"
        }
      }
    }
  }
}
